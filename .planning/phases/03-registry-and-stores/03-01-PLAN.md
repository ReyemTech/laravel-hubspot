---
phase: 03-registry-and-stores
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Registry/HubspotObjectType.php
  - src/Registry/AssociationTypeRegistry.php
  - src/Registry/BaselineAssociationTypes.php
  - src/Registry/Contracts/AssociationTypeStore.php
  - src/Registry/Stores/ArrayAssociationTypeStore.php
  - src/Registry/Stores/CacheAssociationTypeStore.php
  - src/ServiceProvider.php
  - config/hubspot.php
  - tests/Unit/Registry/HubspotObjectTypeTest.php
  - tests/Unit/Registry/BaselineAssociationTypesTest.php
  - tests/Feature/Registry/AssociationTypeRegistryTest.php
  - tests/Feature/Registry/LabelledWriteThroughRegistryTest.php
autonomous: true
requirements: [REG-01, REG-02]

must_haves:
  truths:
    - "A labelled association write resolves its directional type id offline, with no network, no credentials and no database, from the seeded baseline map."
    - "Rebinding one container key is the whole integration: the Gateway's public shape does not change when the real registry replaces the throwing default."
    - "A direction the registry does not know throws, names that direction and the label, and never returns the inverse id."
    - "HubspotObjectType normalises deals, line_items and p_* custom objects to canonical identifiers, and rejects what it cannot normalise rather than passing it through."
    - "inverse_type_id is carried on a row but is unreachable from any write path, proven by a test rather than asserted in a comment."
  artifacts:
    - src/Registry/HubspotObjectType.php
    - src/Registry/AssociationTypeRegistry.php
    - src/Registry/BaselineAssociationTypes.php
    - tests/Feature/Registry/LabelledWriteThroughRegistryTest.php
  key_links:
    - "AssociationTypeRegistry implements Gateway\\Contracts\\AssociationTypeResolver; ServiceProvider rebinds that one key away from UnresolvedAssociationTypeResolver."
    - "The store contract is introduced here with an array and a cache implementation, so 03-02 adds a database store without reopening the registry."
    - "Registry may name Gateway and Exceptions and nothing else (R2) — no HubSpot\\* anywhere in this plan."
---

<objective>
Make a labelled association write work offline. Ship `HubspotObjectType`, the seeded HubSpot-defined
baseline map, and `AssociationTypeRegistry` — the real resolver that replaces Phase 2's honest
throwing default by rebinding a single container key.

Purpose: this is the plan that turns the Phase 2 seam into a working feature. Until it lands, every
labelled association in the package throws. It is also the plan where the fallback this project
exists to forbid could quietly reappear, one layer down from where Phase 2 forbade it.

Output: offline directional resolution for the cited baseline pairs, a miss that throws naming the
direction, object type normalisation, and a store seam that 03-02 extends without touching the
registry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-registry-and-stores/03-CONTEXT.md
@.planning/phases/02-gateway-layer/02-05-SUMMARY.md
@STANDARDS.md
@CLAUDE.md
@src/Gateway/Contracts/AssociationTypeResolver.php
@src/Gateway/UnresolvedAssociationTypeResolver.php
@src/Gateway/AssociationType.php
@src/Gateway/AssociationPair.php
@src/ServiceProvider.php
@config/hubspot.php

## REG-01 has no acceptance criteria in any source document

The specs state `HubspotObjectType`'s job and stop. The roadmap explicitly sanctions deriving
criteria during planning rather than inventing them at roadmap level, so they are derived here and
must be recorded in the summary as derived:

1. `deals`, `Deals`, `deal` and `DEAL` normalise to one canonical identifier; so do `line_items`,
   `lineItems` and `line item`.
2. A `p_*` custom object identifier normalises to itself, case-normalised, and is **not** rejected
   for being unknown — there is no allow-list of custom objects and inventing one would break every
   portal that has any.
3. Anything that cannot be normalised throws `ObjectTypeException`, naming what was passed. It does
   not pass the value through unchanged: a silent pass-through is how a typo reaches a request path
   and comes back as a 404 about a route rather than an error about the argument.
4. Normalisation is total and deterministic — the same input always yields the same output, and the
   canonical form is itself canonical (normalising twice changes nothing).

Resolving the local id column for a bound model is **out of scope**: model binding is Phase 4
(SYNC-01). Say so in the summary rather than stubbing it.

## The rule most at risk in this plan

Phase 2 forbade the Gateway from falling back to the inverse type id. This plan builds the thing
that *holds* both ids, so it is where the fallback can reappear — a lookup that misses the requested
direction and finds the row from the other one is one `??` away. The registry must key on the
direction, not on the pair-as-a-set. Write the test that fails if it ever does.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HubspotObjectType</name>
  <files>src/Registry/HubspotObjectType.php, tests/Unit/Registry/HubspotObjectTypeTest.php</files>
  <read_first>src/Gateway/ObjectRef.php, src/Exceptions/ObjectTypeException.php</read_first>
  <behavior>
    - Normalises the documented aliases of the standard object types to one canonical identifier each.
    - Normalises a `p_*` custom object identifier to itself rather than rejecting it.
    - Throws `ObjectTypeException` naming the input when normalisation is impossible.
    - Normalising an already-canonical identifier returns it unchanged.
  </behavior>
  <action>
Write the RED test first, commit it, confirm it fails, then implement.

`final`, package-owned, no `HubSpot\*`. Follow `ObjectRef`'s merged precedent for parameter typing:
native `mixed` with an explicit `is_string()` check, because `declare(strict_types=1)` binds at the
caller's file and a Laravel consumer file without it coerces silently. Add the named constructor to
`ObjectTypeException` rather than introducing a new exception member — STANDARDS §9 fixes the
hierarchy at five.

Do not build an allow-list of custom object types. Do not resolve model id columns.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Unit/Registry/HubspotObjectTypeTest.php</automated>
    <automated>vendor/bin/phpstan analyse --no-progress</automated>
  </verify>
  <done>Aliases normalise, `p_*` survives, the unnormalisable throws naming itself, and normalisation is idempotent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the baseline map and the store seam</name>
  <files>src/Registry/BaselineAssociationTypes.php, src/Registry/Contracts/AssociationTypeStore.php, src/Registry/Stores/ArrayAssociationTypeStore.php, src/Registry/Stores/CacheAssociationTypeStore.php, config/hubspot.php, tests/Unit/Registry/BaselineAssociationTypesTest.php</files>
  <read_first>.planning/phases/03-registry-and-stores/03-CONTEXT.md, docs/probes/association-inverse-probe.md, config/hubspot.php</read_first>
  <behavior>
    - The baseline map holds the four cited directional pairs, each direction as its own entry with its own type id.
    - Every seeded entry carries from type, to type, type id, category, label, inverse type id and whether it is the default.
    - No entry exists that cannot be cited to the design spec or the probe.
    - A store answers a lookup for one direction and knows nothing about assembling the reverse.
    - The cache store reads through to the baseline on a miss and never writes a guess.
  </behavior>
  <action>
Write the RED test first, commit it, confirm it fails, then implement.

Seed only ids with a citable source — the four pairs in `03-CONTEXT.md` at minimum. **Do not invent
ids to make the map look complete.** An absent id throws, which is safe; a wrong id writes silently,
which is the failure this package exists to prevent. Record in the summary exactly which entries were
seeded and the source for each.

The store contract takes a direction and a label and returns a type or nothing. It must have no
method that takes an unordered pair, and no method that returns "the types for these two objects"
without an order — that signature is how the inverse gets picked.

`config/hubspot.php` gains the store selector (`HUBSPOT_STORE`, default `cache`, as the existing key
already anticipates). Do not add the database store here; 03-02 owns it, and the selector must reject
an unknown store name with a directed error rather than falling back silently.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Unit/Registry/BaselineAssociationTypesTest.php</automated>
    <automated>vendor/bin/pest --coverage --min=95</automated>
  </verify>
  <done>The cited pairs resolve per direction with distinct ids; no uncitable id is seeded; the store contract cannot express an unordered lookup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the registry, and the rebinding that makes labelled writes work</name>
  <files>src/Registry/AssociationTypeRegistry.php, src/ServiceProvider.php, tests/Feature/Registry/AssociationTypeRegistryTest.php, tests/Feature/Registry/LabelledWriteThroughRegistryTest.php</files>
  <read_first>src/Gateway/UnresolvedAssociationTypeResolver.php, src/Gateway/AssociationGateway.php, tests/Feature/Gateway/NeverTheInverseTest.php</read_first>
  <behavior>
    - `AssociationTypeRegistry` implements `AssociationTypeResolver` and resolves a labelled direction from the store.
    - A direction the registry does not know throws `AssociationTypeException` naming the from type, the to type and the label.
    - A registry that knows only the OPPOSITE direction still throws, and issues no request.
    - A labelled association write succeeds end to end through the facade with no network, no credentials and no database.
    - No write path reads `inverse_type_id`.
  </behavior>
  <action>
Write the RED test first, commit it, confirm it fails, then implement.

`ServiceProvider` rebinds `AssociationTypeResolver::class` to `AssociationTypeRegistry`. That single
line is the integration; if anything else in the Gateway has to change, the Phase 2 seam was wrong
and that is a finding to report, not to route around.

`LabelledWriteThroughRegistryTest` is this plan's headline: a labelled write through
`Hubspot::fake()` that resolves a real baseline id and puts it on the wire, asserted on the recorded
request body. It is the proof that the phase's goal is met.

Mirror `NeverTheInverseTest`'s opposite-direction case at the registry level, and assert **zero
requests were issued**, not merely that a throw happened. Then write the inverse-unreachability test:
seed a row whose `inverse_type_id` differs from every `type_id`, exercise every write path, and
assert that value never appears in any recorded request body.

Registry may name `Gateway` and `Exceptions` only. Run `bash scripts/ci/verify-arch-rules-fire.sh`
before you push — `tests/Arch/ResolverSeamTest.php` exists precisely to catch a violation here.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/Registry</automated>
    <automated>bash scripts/ci/verify-arch-rules-fire.sh</automated>
    <automated>vendor/bin/pest --mutate --min=80</automated>
  </verify>
  <done>A labelled write resolves offline and reaches the wire with the directional id; an unknown or opposite-only direction throws with zero requests; `inverse_type_id` is provably unreachable from any write.</done>
</task>

</tasks>

<verification>
- `vendor/bin/pest` — full suite green; the Phase 2 baseline of 354 tests must not regress.
- `vendor/bin/pest --coverage --min=95` (currently 100.0%) and `--mutate --min=80` (currently 98.84%,
  7 documented equivalent survivors). No new survivors.
- `vendor/bin/phpstan analyse --no-progress` — clean, no baseline, no new suppression.
- `vendor/bin/pint --test`, `vendor/bin/phpcs --standard=phpcs.xml -q`.
- `bash scripts/ci/verify-arch-rules-fire.sh` — 10/10, R2 in particular.
- `bash scripts/ci/verify-quality-gates-fire.sh`, `bash scripts/ci/check-source-hygiene.sh`.
</verification>

<success_criteria>
- A labelled association resolves offline from the baseline and reaches the wire with the correct
  directional type id.
- An unknown direction, and a direction known only in reverse, both throw naming the direction and
  issue nothing.
- `HubspotObjectType` normalises the standard aliases and `p_*`, and throws on the unnormalisable.
- `inverse_type_id` is unreachable from every write path, proven by test.
- The Gateway's public shape is unchanged; one container key moved.
</success_criteria>

<output>
Create `.planning/phases/03-registry-and-stores/03-01-SUMMARY.md`.

Record explicitly: the derived REG-01 acceptance criteria and that they were derived, not sourced;
every baseline entry seeded with its citation; and confirmation that rebinding one key was the whole
integration — or, if it was not, exactly what else had to change and why that is a Phase 2 defect.
</output>
