---
phase: 04-model-sync
plan: 09
type: execute
wave: 7
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07"]
files_modified:
  - src/Registry/Console/DoctorCommand.php
  - tests/Feature/Registry/DoctorCommandTest.php
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [REG-04b, REG-01b]

must_haves:
  truths:
    - "`php artisan hubspot:doctor` reports every bound model, whether it soft-deletes, and what its delete policy resolves to (ROADMAP Phase 3 SC4, REG-04b)."
    - "`DoctorCommandTest::test_it_names_the_bound_model_section_as_not_built_rather_than_omitting_it` is replaced in the same change that makes the section real, so the suite never asserts the opposite of what ships."
    - "With no bindings configured the command reports that none are configured and names the config key to add — a true statement now that binding exists, unlike the not-built wording it replaces."
    - "Reporting is still not failing: `handle()` returns success unconditionally, because every state it reports is a legitimate state of an installation."
    - "REG-01 and REG-04 tick here — both halves of each are now shipped — and SYNC-01a through SYNC-05 are marked with what actually landed."
  artifacts:
    - src/Registry/Console/DoctorCommand.php with a real bound-model section
    - tests/Feature/Registry/DoctorCommandTest.php with the absent-section test replaced
    - .planning/REQUIREMENTS.md with REG-01, REG-04 and the SYNC requirements ticked
  key_links:
    - "`DoctorCommand` reads `config('hubspot.models')` through `Sync\\ModelBindings` — data, not a layer dependency; R2 permits `Registry` to depend on `Gateway`, the package exceptions and the framework, and NOT on `Sync`, so verify the arch suite rather than assuming"
    - "The resolved delete policy comes from `Sync\\DeletePolicy` over primitives, so the command reports what will actually happen rather than re-deriving it"
    - "Soft-delete detection uses `class_uses_recursive()` or the `forceDelete` method, never an inspection of a `deleted_at` value"
---

<objective>
Make `hubspot:doctor`'s bound-model section real, and close REG-01 and REG-04.

Phase 3 shipped the command NAMING that section as not built — three lines saying it is absent, that
its emptiness is not a statement about the reader's models, and what it will report once it exists —
with a test holding exactly that. Model binding now exists. The command must report it, and that test
must change in this same plan, or the suite asserts the opposite of what ships.

Purpose: REG-04b, the last of the two requirement halves Phase 3 deliberately left open, plus the
phase close-out that ticks what actually landed.
Output: a real bound-model report, a replaced test, and requirement records that match the code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-model-sync/04-CONTEXT.md
@.planning/phases/04-model-sync/04-RESEARCH.md
@.planning/phases/04-model-sync/04-PATTERNS.md
@.planning/phases/04-model-sync/04-06-SUMMARY.md
@CLAUDE.md
@STANDARDS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — replace the absent-section test with assertions against a real report</name>
  <files>tests/Feature/Registry/DoctorCommandTest.php</files>
  <read_first>
    - `tests/Feature/Registry/DoctorCommandTest.php` — the whole file, especially the class docblock's REG-04a/REG-04b split language and `test_it_names_the_bound_model_section_as_not_built_rather_than_omitting_it` at line 162
    - `src/Registry/Console/DoctorCommand.php` lines 13-42 (the docblock stating the split) and 106-117 (`reportTheSectionThatDoesNotExistYet()`)
    - `tests/Support/CommandOutput.php` — `linesOf()`, the console-output assertion helper
    - `src/Sync/ModelBindings.php` and `src/Sync/DeletePolicy.php` — what the report reads
    - `04-PATTERNS.md` § "src/Registry/Console/DoctorCommand.php (REG-04b edit)" — the exact code being replaced and the instruction to keep the split language while flipping which half is done
  </read_first>
  <behavior>
    - With three bound models configured, the report names each model class exactly once.
    - For each bound model it reports the object type it binds to and the `id_property` it upserts on.
    - For each bound model it reports whether the model uses `SoftDeletes`.
    - For each bound model it reports what its delete policy resolves to, taken from `DeletePolicy` rather than re-derived.
    - A soft-deleting bound model and a non-soft-deleting one report different resolved policies under the same configuration, so the report is proven to depend on the model rather than on the config alone.
    - With no bindings configured, the report says none are configured and names the config key to add. It does NOT say the feature is unbuilt.
    - The command still returns success in every case.
  </behavior>
  <action>
Delete `test_it_names_the_bound_model_section_as_not_built_rather_than_omitting_it` and the three
`assertContains` calls inside it. That test held a true statement about a package that could not bind
models; keeping it after this change would make the suite assert the opposite of what ships, which is
the specific failure `04-CONTEXT.md` and `04-VALIDATION.md` both flag.

Rewrite the class docblock's REG-04a/REG-04b paragraphs rather than deleting them: keep the split
language, flip which half is done, and say plainly that the section is now real and REG-04 closes here.
The record of why the split existed is worth more than a tidy file.

Add the behaviours above as named tests. The two that carry the weight:

1. The three-bound-models test, using `defineEnvironment()` to configure three bindings with different
   object types and `id_property` values, asserting each model class appears exactly once with its own
   object type and property. A report that printed one line per binding but reused one model's values
   would pass a laxer assertion.
2. The soft-delete-distinguishing test: two bound models under one configuration, one applying
   `SoftDeletes` and one not, asserting the two resolved policies differ. A report that read only
   `config('hubspot.auto_sync.hard_delete')` and printed it per model would pass every other test in
   this file and be wrong about exactly the thing REG-04b asks for.

Reuse `tests/Support/Sync/SoftDeletingLead.php` and the bound models 04-04 and 04-05 created; do not
add new ones.

Commit RED.
  </action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/Registry/DoctorCommandTest.php; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/pest tests/Feature/Registry/DoctorCommandTest.php` exits non-zero.
    - `grep -c 'test_it_names_the_bound_model_section_as_not_built_rather_than_omitting_it' tests/Feature/Registry/DoctorCommandTest.php` returns 0.
    - `grep -c 'REG-04b' tests/Feature/Registry/DoctorCommandTest.php` returns at least 1 — the split language survives, flipped.
    - `grep -c 'SoftDeletes' tests/Feature/Registry/DoctorCommandTest.php` returns at least 1.
    - `git log -1 --format=%s` matches `^test(04-09): `.
  </acceptance_criteria>
  <done>The absent-section test is gone, the real report's behaviour is asserted including the soft-delete distinction, and the suite is red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the real bound-model section, and the phase close-out records</name>
  <files>src/Registry/Console/DoctorCommand.php, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `tests/Feature/Registry/DoctorCommandTest.php` — the contract just committed
    - `src/Registry/Console/DoctorCommand.php` — the reporting style of the four sections that already ship, especially how each line states both a configured value and what is actually bound
    - `src/Sync/DeletePolicy.php` — the resolver's primitive signature and its action enum
    - `.planning/REQUIREMENTS.md` REG-01 (line ~238), REG-04 (line ~341), SYNC-01a…SYNC-05 (from line ~366) and the traceability table (from line ~774)
    - `.planning/ROADMAP.md` Phase 3's close-out note and Phase 4's success criteria and Progress table
  </read_first>
  <behavior>Every test from task 1 passes with no test edited.</behavior>
  <action>
Replace `reportTheSectionThatDoesNotExistYet()` with a real bound-model report. Per bound model, one
group of lines carrying: the model class, the object type it binds to, the `id_property` it upserts
on, whether it uses `SoftDeletes`, and what its delete policy resolves to. Resolve the policy through
`Sync\DeletePolicy` over primitives rather than re-deriving it here — a doctor that computed its own
answer would report what the reader hopes is happening rather than what will happen, which is the
failure mode a diagnostic exists to prevent.

Detect `SoftDeletes` with `class_uses_recursive()` or the presence of the `forceDelete` method, never
by inspecting a `deleted_at` value: a model with a null `deleted_at` and a model with no soft deletes
at all are different facts.

With no bindings, print that none are configured and name the config key to add. That is a true
statement now, which the wording it replaces was not.

Keep the command's existing contract exactly: it reads only local state — no network, no credentials —
and `handle()` returns success unconditionally, because every state it reports is a legitimate state of
an installation and a non-zero exit would make it useless in a scripted health check.

Watch the layer boundary. `Registry` may depend on `Gateway`, the package exceptions and the framework
— not on `Sync`. `04-RESEARCH.md`'s responsibility map argues the command reads a `Sync`-introduced
CONFIG KEY, which is data rather than a class dependency, and R2 is untouched. Referencing
`Sync\ModelBindings` or `Sync\DeletePolicy` from `Registry\Console\DoctorCommand` is a class
dependency and R2 will fail on it. Resolve this properly rather than by widening R2: read the binding
and the policy through container-resolved contracts, or move the resolution behind an interface the
`Registry` layer may name. Run `vendor/bin/pest tests/Arch` before assuming either way, and record what
you found in the summary — this is the one architectural question this plan carries.

Then close the phase's records, in a separate `docs(04-09):` commit so the PR-branch filter drops them
cleanly:

- `.planning/REQUIREMENTS.md`: tick REG-01 and REG-04, with progress notes naming what each half
  shipped and in which plan. Tick SYNC-01a, SYNC-02, SYNC-03, SYNC-04 and SYNC-05 with the same
  discipline the Phase 3 entries use — what landed, where, and anything the acceptance text invited
  that the code deliberately does not do. SYNC-01b stays open and stays Phase 9's. Update the
  traceability table rows.
- `.planning/ROADMAP.md`: mark Phase 4's plans complete in the Progress table and add a phase
  close-out note in the shape Phase 3's carries, naming anything that did not close and why.
  Phase 4 SC2's wording about different id columns was already corrected at planning time under D-13;
  do not reintroduce it.
  </action>
  <verify>
    <automated>vendor/bin/pest && bash scripts/ci/verify-arch-rules-fire.sh</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/pest tests/Feature/Registry/DoctorCommandTest.php` is green.
    - `vendor/bin/pest` full suite is green.
    - `vendor/bin/pest --coverage --min=95` and `vendor/bin/pest --mutate --min=80` both exit 0.
    - `vendor/bin/pest tests/Arch` is green and `bash scripts/ci/verify-arch-rules-fire.sh` exits 0.
    - `grep -c 'reportTheSectionThatDoesNotExistYet' src/Registry/Console/DoctorCommand.php` returns 0.
    - `grep -c 'class_uses_recursive\|forceDelete' src/Registry/Console/DoctorCommand.php` returns at least 1.
    - `php artisan` is not available here, so assert the exit code through the test suite: `grep -c 'Command::SUCCESS' tests/Feature/Registry/DoctorCommandTest.php` returns at least 1 and that test passes.
    - `grep -c '\[x\] \*\*REG-01\*\*' .planning/REQUIREMENTS.md` returns 1 and the same for `REG-04`.
    - `grep -c 'SYNC-01b' .planning/REQUIREMENTS.md` returns at least 2 and SYNC-01b is NOT ticked.
    - `git log --format=%s -3` shows `test(04-09):` strictly before `feat(04-09):`, plus a separate `docs(04-09):` commit.
  </acceptance_criteria>
  <done>`hubspot:doctor` reports every bound model with its soft-delete status and resolved delete policy, the test that held the opposite is gone, and REG-01 and REG-04 tick with SYNC-01b left open for Phase 9.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic output → operator belief | An operator acts on what the doctor says; a confidently wrong line is worse than no line |
| `Registry` → `Sync` | A layer boundary the architecture tests enforce, which this report's data source sits across |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-37 | Repudiation | a test asserting the opposite of what ships | high | mitigate | The absent-section test is deleted in the same change that makes the section real, which is why `04-CONTEXT.md` requires both in one plan |
| T-04-38 | Tampering | a report re-deriving the delete policy | high | mitigate | The resolved policy comes from `Sync\DeletePolicy`, the same resolver the observer uses. A doctor with its own copy of the logic reports the hoped-for answer, not the actual one |
| T-04-39 | Repudiation | soft-delete detection by `deleted_at` inspection | medium | mitigate | `class_uses_recursive()` or the `forceDelete` method; a null `deleted_at` and no soft deletes at all are different facts and the report must not conflate them |
| T-04-40 | Elevation of Privilege | `Registry` gaining a class dependency on `Sync` | high | mitigate | Resolve through container-bound contracts rather than widening R2; the arch suite and `verify-arch-rules-fire.sh` are the gate, and what was found is recorded in the summary |
</threat_model>

<phase_artifacts>
## Artifacts this phase produces

Every symbol Phase 4 creates, and the plan that owns it. A plan may READ anything already shipped; it
may CREATE or MODIFY only what names it below.

### `src/Sync/`

| Symbol | Kind | Owner |
|---|---|---|
| `Sync\SyncsToHubspot` | trait (D-05) | 04-02 |
| `…::hubspotLink()`, `…::hubspotId()`, `…::syncToHubspot()` | MorphOne relation + accessors (D-06, REG-01b) | 04-02, scoped in 04-04 |
| `…::scopeWhereHubspotId()`, `…::scopeSyncedToHubspot()`, `…::scopePendingHubspotSync()` | query scopes (D-06) | 04-04 |
| `…` per-model `$hubspotAutoSync` reader | auto-sync override (SYNC-03) | 04-05 |
| `…::syncManyToHubspot(iterable $models): void` | static collection entry point (D-16) | 04-08 |
| `Sync\HubspotObjectLink` | Eloquent model over `hubspot_object_links` (D-13) | 04-02 |
| `Sync\ModelBinding`, `Sync\ModelBindings` | binding value + config validator (D-12) | 04-02, 04-04 |
| `Sync\PropertyMapper` | final pure transform (SYNC-02) | 04-02 skeleton → 04-03 full |
| `Sync\HubspotObserver` | final generic observer, binding resolved per call | 04-02 → 04-05 → 04-06 → 04-07 |
| `Sync\SyncHubspotObjectJob` | final `ShouldQueue` (D-07…D-11) | 04-02, 04-03, 04-06, 04-07 |
| `Sync\SyncHubspotObjectsBatchJob` | final `ShouldQueue`, one `upsertMany()` (D-16) | 04-08 |
| `Sync\ArchiveHubspotObjectJob` | final `ShouldQueue` (SYNC-04) | 04-06, 04-07 |
| `Sync\DeletePolicy` | final pure resolver over primitives (SYNC-04) | 04-06 |
| `Sync\SyncGate` | final; suppression, kill switch, environment (SYNC-05) | 04-07 |

### Outside `src/Sync/`

| Artifact | Owner |
|---|---|
| `database/migrations/sync/0001_01_01_000000_create_hubspot_object_links_table.php` (D-13, D-18) | 04-02 |
| `ServiceProvider::migrationGroups()` second entry; `ModelBindings` binding; observer attachment | 04-02 |
| `ConfigurationException::missingIdProperty()`, `::idPropertyNotMapped()` (D-12) | 04-02 |
| `ConfigurationException::unboundSyncModel()` | 04-04 |
| `ConfigurationException::unknownHardDeletePolicy()`, `::unknownRestorePolicy()` | 04-06 |
| `HubspotManager::withoutSyncing()`, `::syncingSuppressed()`, `::isFaked()` (SYNC-05) | 04-07 |
| `HubspotManager::assertSynced()` widened to accept a bound model | 04-08 |
| `DoctorCommand`'s real bound-model section (REG-04b) | **04-09** |
| `scripts/ci/check-vendor-namespaces.sh` + two violation fixtures; quality.yml `vendor-namespaces`; ci.yml `manifest` renamed (D-03, D-04) | 04-01 |

### New config keys in `config/hubspot.php`

| Key | Owner |
|---|---|
| `hubspot.models` (D-13, supersedes `id_column`) | 04-02, documented further in 04-04 |
| `hubspot.auto_sync.enabled`, `.on`, `.queue` | 04-05 |
| `hubspot.auto_sync.hard_delete`, `.on_restore` | 04-06 |
| `hubspot.disabled` — existing; 04-07 adds the testing-environment default as RUNTIME logic, never a closure | 04-07 |

### New test files

| File | Requirement | Owner |
|---|---|---|
| `tests/Ci/Fixtures/VendorNamespaces/*` | — (D-04) | 04-01 |
| `tests/Support/Sync/SyncTestCase.php`, `SyncedLead.php`, `tests/Feature/Sync/TracerSyncTest.php` | SYNC-01a, SYNC-03 | 04-02 |
| `tests/Unit/Sync/PropertyMapperTest.php`, `tests/Feature/Sync/UpdateSyncTest.php`, `tests/Support/Sync/MappedDeal.php`, `MappedStage.php` | SYNC-02 | 04-03 |
| `tests/Feature/Sync/ModelBindingTest.php`, `tests/Unit/Sync/SyncsToHubspotTraitTest.php`, `tests/Support/Sync/MultiBindingTestCase.php`, `SyncedContact.php`, `SyncedIntake.php` | SYNC-01a, REG-01b | 04-04 |
| `tests/Feature/Sync/AutoSyncBootTest.php`, `tests/Support/Sync/SoftDeletingLead.php` | SYNC-03 | 04-05 |
| `tests/Unit/Sync/DeletePolicyTest.php`, `tests/Feature/Sync/DeletePolicyTest.php` | SYNC-04 | 04-06 |
| `tests/Feature/Sync/SyncSuppressionTest.php` | SYNC-05 | 04-07 |
| `tests/Feature/Sync/BatchSyncTest.php` | SYNC-03 (batch half) | 04-08 |

### Edited, never created

| File | What changes | Owner |
|---|---|---|
| `tests/Ci/ComposerManifestTest.php` | count assertion → vendor allow-list (D-03) | 04-01 |
| `tests/Arch/LayerBoundariesTest.php`, `tests/Arch/rules.json` | R3 gains `Illuminate` (D-01) | 04-01 |
| `tests/Ci/MigrationPublishingTest.php` | a second migration group exists | 04-02 |
| `src/Testing/HubspotFake.php`, `src/Testing/RequestLog.php` | `assertSynced` resolves a bound model | 04-08 |
| `tests/Feature/Registry/DoctorCommandTest.php` | the absent-section test at line 162 is replaced (REG-04b) | **04-09** |
| `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` | phase close-out; REG-01 and REG-04 tick | **04-09** |
</phase_artifacts>

<verification>
1. `vendor/bin/pint`
2. `vendor/bin/phpstan analyse`
3. `vendor/bin/phpcs --standard=phpcs.xml -q`
4. `vendor/bin/pest`
5. `vendor/bin/pest --coverage --min=95`
6. `vendor/bin/pest --mutate --min=80`
7. `bash scripts/ci/check-source-hygiene.sh --self-test`
8. `bash scripts/ci/check-vendor-namespaces.sh --self-test`
9. `bash scripts/ci/verify-arch-rules-fire.sh`
10. `bash scripts/ci/verify-quality-gates-fire.sh`
11. `composer validate --strict`

This is the phase's last plan: run the whole list, not a subset. Push, read GitHub's own result, and
merge only when a completed Codex review names the head commit.
</verification>

<success_criteria>
- `hubspot:doctor` reports every bound model, its object type and `id_property`, whether it soft-deletes, and its resolved delete policy.
- The absent-section test is gone, replaced in the same plan that made the section real.
- With no bindings the report says none are configured rather than that the feature is unbuilt.
- The command still exits successfully in every state it reports.
- `Registry` gained no class dependency on `Sync`, or the accommodation is recorded.
- REG-01 and REG-04 tick; SYNC-01b stays open and stays Phase 9's.
- Full suite green, coverage ≥95%, MSI ≥80%, every gate proven to still fire.
</success_criteria>

<output>
Create `.planning/phases/04-model-sync/04-09-SUMMARY.md` when done. Record how the `Registry` → `Sync`
data dependency was resolved without widening R2, and the final full-suite test and assertion counts
against the 668 / 2567 baseline.
</output>
