---
phase: 05-inbound-webhooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Gateway/Contracts/WebhookGatewayContract.php
  - src/Gateway/WebhookGateway.php
  - src/Webhooks/NormalizedWebhookEvent.php
  - src/Webhooks/Events/HubspotWebhookReceived.php
  - src/Webhooks/ProcessWebhookEventJob.php
  - src/Webhooks/RouteRegistrar.php
  - src/Webhooks/WebhookController.php
  - src/ServiceProvider.php
  - tests/Feature/Webhooks/InboundWebhookTracerTest.php
  - tests/Feature/Webhooks/InboundWebhookFailureTest.php
  - tests/Arch/WebhookBoundaryTest.php
autonomous: true
requirements:
  - HOOK-01
must_haves:
  truths:
    - "A consumer adds Route::hubspotWebhook('hubspot/webhook') and a correctly signed POST queues one job per item before returning 204."
    - "Signature verification receives the original URI, including unsorted query order, and delegates to the HubSpot SDK inside Gateway."
    - "Invalid signatures are rejected before JSON parsing or dispatch; signed malformed input returns 400; any queue handoff failure returns 500."
    - "Signed HTTP receipt is independent of the HubSpot app model; management credentials never participate in receipt."
  artifacts:
    - path: src/Webhooks/RouteRegistrar.php
      provides: "The Route::hubspotWebhook macro"
    - path: src/Webhooks/WebhookController.php
      provides: "Verify-before-parse HTTP adapter and batch handoff"
    - path: src/Gateway/WebhookGateway.php
      provides: "The only SDK-backed signature adapter"
    - path: src/Webhooks/ProcessWebhookEventJob.php
      provides: "One queued unit of work per normalized item"
  key_links:
    - from: src/Webhooks/WebhookController.php
      to: src/Gateway/Contracts/WebhookGatewayContract.php
      via: "raw method, externally visible base URL plus untouched REQUEST_URI, raw body, signature, and timestamp"
    - from: src/Webhooks/WebhookController.php
      to: src/Webhooks/ProcessWebhookEventJob.php
      via: "one successful Laravel bus dispatch per validated batch item"
    - from: src/ServiceProvider.php
      to: src/Webhooks/RouteRegistrar.php
      via: "boot-time macro registration"
---

<objective>
Ship the production-quality end-to-end receipt tracer for HOOK-01: one signed HubSpot HTTP item
travels through the route macro, Gateway-owned SDK verification, normalization, queued job, and
generic Laravel event. Expand that slice to all batch and failure statuses per D-13, D-14, and D-15.

Purpose: Prove the public one-line route and the security-critical handoff before persistence and
typed-routing expansions are layered onto it. Receipt accepts signed HTTP from every app model per
D-16; app-model and subscription-management credentials are deliberately absent from this path.

Output: Route macro, raw-request adapter, Gateway verifier, normalized event, queued job, generic
event, provider wiring, and focused security/HTTP tests.
</objective>

<execution_context>
@/Users/mariomeyer/.codex/gsd-core/workflows/execute-plan.md
@/Users/mariomeyer/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-inbound-webhooks/05-CONTEXT.md
@.planning/phases/05-inbound-webhooks/05-RESEARCH.md
@.planning/phases/05-inbound-webhooks/05-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one signed webhook from HTTP to a queued generic event</name>
  <files>src/Gateway/Contracts/WebhookGatewayContract.php, src/Gateway/WebhookGateway.php, src/Webhooks/NormalizedWebhookEvent.php, src/Webhooks/Events/HubspotWebhookReceived.php, src/Webhooks/ProcessWebhookEventJob.php, src/Webhooks/RouteRegistrar.php, src/Webhooks/WebhookController.php, src/ServiceProvider.php, tests/Feature/Webhooks/InboundWebhookTracerTest.php</files>
  <read_first>
    <file>src/Gateway/Contracts/AssociationDefinitionsGatewayContract.php</file>
    <file>src/Gateway/AssociationDefinitionsGateway.php</file>
    <file>src/Sync/SyncHubspotObjectJob.php</file>
    <file>src/ServiceProvider.php</file>
    <file>tests/TestCase.php</file>
  </read_first>
  <behavior>
    - A test signed over an intentionally unsorted query string fails until the exact externally visible scheme/host plus untouched server REQUEST_URI is passed to the verifier.
    - A valid one-item JSON array dispatches exactly one ProcessWebhookEventJob, returns 204, and running that job emits HubspotWebhookReceived carrying the same immutable NormalizedWebhookEvent.
    - The normalized object exposes eventId as an opaque string, subscriptionType, portalId, appId, objectId, occurredAt as DateTimeImmutable, attemptNumber, and normalized optional property/association/lifecycle fields; it exposes no SDK type.
    - The same signed request is accepted regardless of configured legacy-public, legacy-private, or project app model.
  </behavior>
  <action>Follow RED then GREEN commits. Add a package-owned WebhookGatewayContract whose verification input is method, original absolute URI, raw body, signature version/value, and timestamp. Implement it in Gateway with HubSpot\Utils\Signature::isValid(); do not implement HMAC or timestamp comparison in Webhooks and do not let SDK classes cross the contract. Build the absolute URI from the framework's trusted scheme/host plus the untouched REQUEST_URI server value so query ordering is preserved; never reconstruct the query from parsed parameters. Register Route::hubspotWebhook(string $uri) during provider boot and route POST requests to an invokable controller. The controller verifies before decoding, normalizes each array item, and dispatches one serializable ProcessWebhookEventJob. The job's tracer behavior emits HubspotWebhookReceived around the exact NormalizedWebhookEvent instance. Keep subscription-management credentials out of every receipt signature and constructor per D-16. Preserve the provider's on-demand client-secret redaction boundary.</action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/Webhooks/InboundWebhookTracerTest.php -x</automated>
  </verify>
  <acceptance_criteria>
    - The unsorted-query test is demonstrably red against a parsed/fullUrl-style URI and green against REQUEST_URI preservation.
    - The SDK utility is named only by Gateway, while Webhooks uses package-owned values and contracts.
    - Route macro registration requires no package routes file and the successful tracer returns 204 only after its job is handed to the bus.
  </acceptance_criteria>
  <done>One correctly signed event reaches a queued generic Laravel event through the public route macro, with no live network request and no app-model restriction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete batch, authentication, malformed-input, and handoff failure behavior</name>
  <files>src/Webhooks/WebhookController.php, src/Webhooks/RouteRegistrar.php, tests/Feature/Webhooks/InboundWebhookFailureTest.php, tests/Arch/WebhookBoundaryTest.php</files>
  <read_first>
    <file>tests/Arch/LayerBoundariesTest.php</file>
    <file>tests/Arch/SdkSurfaceTest.php</file>
    <file>tests/Arch/SecretLoggingTest.php</file>
    <file>config/hubspot.php</file>
  </read_first>
  <behavior>
    - Missing, stale, or invalid signatures return 401 before JSON decoding, event emission, handler work, or queue dispatch per D-13.
    - A validly signed body that is invalid JSON, is not a list, or contains an invalid item returns 400 and logs only an error code/count/route name, never body, signature, or secret per D-13.
    - A valid batch of N items dispatches N jobs and returns 204; if any dispatch throws, the response is 500 per D-14 so HubSpot can redeliver the batch.
    - With D-15's enforce switch false, unsigned local-development traffic is accepted and a payload-free warning names the unsafe bypass.
  </behavior>
  <action>Add strict shape validation after signature success, complete the deterministic 401/400/500/204 mapping, and cover zero-, one-, and multi-item batches. A queue failure must escape the handoff loop to the controller response; do not acknowledge a partially handed-off batch. Implement D-15 only as the existing enforcement flag's explicit bypass and issue a warning whose message/context contains no body, signature, client secret, or payload-derived value. Extend architecture/security coverage so Webhooks cannot reference HubSpot\ classes and the raw payload cannot enter logs. Keep all default-suite collaborators fake and prohibit real portal traffic.</action>
  <verify>
    <automated>vendor/bin/pest tests/Feature/Webhooks/InboundWebhookFailureTest.php tests/Arch/WebhookBoundaryTest.php -x</automated>
  </verify>
  <acceptance_criteria>
    - All D-13 statuses are asserted together with zero dispatch on rejection.
    - D-14 is asserted with a throwing bus and a 500 response, not merely an exception unit test.
    - D-15's warning is asserted payload-free, and enforcement remains true by default.
  </acceptance_criteria>
  <done>The route fails closed, preserves safe diagnostics, and acknowledges only completely handed-off valid batches.</done>
</task>

</tasks>

<artifacts>
- `src/Gateway/Contracts/WebhookGatewayContract.php` and `src/Gateway/WebhookGateway.php` — package boundary and SDK-backed validation.
- `src/Webhooks/RouteRegistrar.php` and `src/Webhooks/WebhookController.php` — one-line route and HTTP status contract.
- `src/Webhooks/NormalizedWebhookEvent.php`, `src/Webhooks/Events/HubspotWebhookReceived.php`, and `src/Webhooks/ProcessWebhookEventJob.php` — first complete receipt path.
- `tests/Feature/Webhooks/InboundWebhookTracerTest.php` and `InboundWebhookFailureTest.php` — executable receipt contract.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HubSpot/client -> webhook route | Untrusted headers, URI, and body enter the package. |
| Webhooks -> Gateway | Raw signature inputs cross into the sole SDK-owning layer. |
| HTTP process -> queue | Validated items become durable asynchronous work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | WebhookController | critical | mitigate | Fail closed with SDK signature verification and 300-second tolerance before parsing or dispatch. |
| T-05-02 | Tampering | Raw URI construction | high | mitigate | Preserve REQUEST_URI byte ordering and pin unsorted-query behavior in an HTTP test. |
| T-05-03 | Information Disclosure | Failure logging | high | mitigate | Log fixed diagnostic fields only; tests reject body, signature, and secret disclosure. |
| T-05-04 | Denial of Service | Batch handoff | medium | mitigate | Strictly validate list/items and return 500 whenever full queue handoff is not achieved. |
| T-05-05 | Elevation of Privilege | Enforcement bypass | high | mitigate | Default enforcement true and make bypass explicit, warning-bearing, and documented for local development only. |
</threat_model>

<verification>
- `vendor/bin/pest tests/Feature/Webhooks/InboundWebhookTracerTest.php tests/Feature/Webhooks/InboundWebhookFailureTest.php tests/Arch/WebhookBoundaryTest.php -x`
- `vendor/bin/pest tests/Arch/LayerBoundariesTest.php tests/Arch/SdkSurfaceTest.php tests/Arch/SecretLoggingTest.php -x`
- `vendor/bin/pest --mutate --parallel --min=80 --class="$(bash scripts/ci/mutation-scope.sh origin/main)"`
</verification>

<success_criteria>
- HOOK-01's public route, raw-URI SDK verification, one-job-per-item batch handoff, and occurredAt exposure are executable.
- The default path performs zero live network I/O and adds no dependency.
- D-13, D-14, D-15, and receipt half of D-16 are directly covered by feature tests.
</success_criteria>

<output>
Create `.planning/phases/05-inbound-webhooks/05-01-SUMMARY.md` when done.
</output>
